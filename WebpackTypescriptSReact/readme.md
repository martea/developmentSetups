# Web setup Webpack React Typescript
to get started install webpack globally and then install dependencies and then hit webpack to generate files that are used.
```
>npm install webpack -g
```

```
>npm install
```

```
>webpack --watch
```